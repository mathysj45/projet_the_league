<section class="player-performances">
    <h3>SES PERFORMANCES</h3>

    <?php if (!empty($stats)) { ?>
        
        <table class="stats-table">
            <thead>
                <tr>
                    <th>Match</th>
                    <th>Points</th>
                    <th>Assists</th>
                </tr>
            </thead>
            <tbody>
                <?php 
                $totalPoints = 0; 
                
                foreach($stats as $stat) { 
                    $totalPoints += $stat->getPoints(); 
                ?>
                <tr>
                    <td><?= $stat->getGameName() ?></td>
                    
                    <td><?= $stat->getPoints() ?></td>
                    <td><?= $stat->getAssists() ?></td>
                </tr>
                <?php } ?>
            </tbody>
        </table>

        <div class="stats-total">
            <strong>Points Totaux sur la carrière : <?= $totalPoints ?></strong>
        </div>

    <?php } else { ?>
        <p class="no-stats">Aucune statistique disponible.</p>
    <?php } ?>
</section>