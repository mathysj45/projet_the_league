<section class="player-performances">
    <h3 class="section-title-blue">SES PERFORMANCES</h3>

    <?php if (!empty($stats)) { ?>
        
        <table class="stats-table">
            <thead>
                <tr>
                    <th class="col-yellow">TEAM ADVERSE</th>
                    <th>POINTS</th>
                    <th>ASSISTS</th>
                    <th>VICTOIRE ?</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach($stats as $stat) { ?>
                <tr>
                    <td class="col-yellow"><?= $stat->getGameName() ?></td>
                    
                    <td><?= $stat->getPoints() ?></td>
                    
                    <td><?= $stat->getAssists() ?></td>
                    
                    <td class="col-green">-</td> 
                </tr>
                <?php } ?>
            </tbody>
        </table>

    <?php } else { ?>
        <p class="no-stats">Aucune performance enregistrée.</p>
    <?php } ?>
</section>