<section class="matchs">
<h2>LES MATCHS</h2>
    <?php foreach($data['games'] as $game)
        { ?>
    <div class="match">
        <div class="match_img">
            <img src='./assets/images/' alt="all players">
            <img src='./assets/images/' alt="all players">
        </div>
        <h3><?= $game['name'] ?></h3>
        <button type="submit">Détails du match<?= $player['name'] ?></button>
        <?php } ?>
    </div>
</section>