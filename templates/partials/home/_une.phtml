<main>
    <section class="team_une">
        <h2>TEAM À LA UNE</h2>
        <div class="row_une"> 
            <p><?= $teams[0]->getLogo(); ?></p>
            <img class="img_une" src="<?= $teams[0]->getLogo(); ?>" alt="">

            <div class="column_une">
                <h3><?= $teams[0]->getName(); ?></h3>
                <p>Les players</p>
                <?php foreach ($teams[0]->getPlayers() as $player){ ?>
                    
                    <p><?= $player->getNickname() ?></p>
                    <img src="<?= $player->getPortrait() ?>" alt="" >
                <?php } ?>

    </section>

    <section class="player_une">
        <h2>LES PLAYERS À LA UNE</h2>
        <div class="row_une"> 
            <?php foreach($data['players'] as $player_une)
                { ?>
            <div class="player_tuiles">
                <img class="img_perso_une" src='./assets/images/' alt="players_une">
                <h3><?=$players[]->getNickname()?></h3>
                <h4><?= $player_une['team']?></h4>
                <?php } ?>
            </div>
        </div>
    </section>
</main>